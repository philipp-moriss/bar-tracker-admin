export interface AnswerSummary {
    id: number;
    // Добавьте остальные поля в зависимости от ответа API
}

export interface StatisticWithQuestions {
    id: number;
    // Добавьте остальные поля в зависимости от ответа API
} 